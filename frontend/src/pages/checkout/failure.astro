---
import ShopLayout from '../../layouts/ShopLayout.astro'
import '../../styles/checkout-result.css'
---

<ShopLayout title="Pago fallido">
  <div class="payment-result-page container">
    <div class="result-card failure">
      <div class="result-icon">✕</div>
      <h1 class="result-title">Pago rechazado</h1>
      <p class="result-message">Tu pago fue rechazado. Por favor intenta nuevamente o usa otro método de pago.</p>
      <a href="/checkout" class="btn btn-primary">Volver al checkout</a>
    </div>
  </div>
</ShopLayout>

<style>
  .payment-result-page {
    padding-top: calc(var(--header-height) + var(--space-8));
    padding-bottom: var(--space-16);
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 60vh;
  }

  .result-card {
    background: var(--color-white);
    border: 1.5px solid var(--color-gray-200);
    border-radius: var(--radius-lg);
    padding: var(--space-12);
    text-align: center;
    max-width: 480px;
    width: 100%;
  }

  .result-card.failure {
    border-color: #ef4444;
    background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%);
  }

  .result-icon {
    width: 72px;
    height: 72px;
    border-radius: 50%;
    background: #ef4444;
    color: white;
    font-size: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--space-6);
  }

  .result-title {
    font-family: var(--font-serif);
    font-size: 1.75rem;
    font-weight: 600;
    color: var(--color-black);
    margin-bottom: var(--space-3);
  }

  .result-message {
    color: var(--color-gray-600);
    margin-bottom: var(--space-6);
  }
</style>